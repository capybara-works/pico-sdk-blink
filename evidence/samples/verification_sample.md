# Verification Summary

Generated: 2026-06-12T00:00:00Z (UTC)

## Overall Status

**partial**

## Evidence Files

- `build_result.json` (present)
- `build.log` (present)
- `ctest_result.json` (present)
- `ctest.log` (present)
- `wokwi_result.json` (present)
- `wokwi.log` (present)
- `flash_result.json` (present)
- `flash.log` (present)
- `hil_result.json` (present)
- `hil.log` (present)
- `uart_result.json` (present)
- `uart.log` (present)
- `gdb_snapshot.json` (present)
- `gdb_snapshot.log` (present)
- `logic_uart_result.json` (present)
- `logic_uart_decode.txt` (present)
- `logic_uart_text.txt` (present)
- `logic_i2c_result.json` (present)
- `logic_i2c_decode.txt` (present)

## Results

- Build: pass
- CTest: pass
- Wokwi: skip — WOKWI_CLI_TOKEN is not set.
- Flash: skip — hardware not enabled. Set PICO_HARDWARE=1 to enable hardware operations.
- HIL: skip — hardware not configured
- UART: skip — hardware not enabled. Set PICO_HARDWARE=1 to enable hardware operations.
- GDB: skip — hardware not enabled. Set PICO_HARDWARE=1 to enable hardware operations.
- Logic Analyzer UART: stub — logic analyzer UART not enabled. Set PICO_LOGIC_UART=1 to enable UART capture, or PICO_LOGIC_ANALYZER=1 to enable all logic captures. Using sample decode output.
- Logic Analyzer I2C: stub — logic analyzer I2C not enabled. Set PICO_LOGIC_I2C=1 to enable I2C capture, or PICO_LOGIC_ANALYZER=1 to enable all logic captures. Using sample decode output.

## Notes

- Wokwi: スキップ (WOKWI_CLI_TOKEN is not set.)。前提条件がないため未実行です。
- Flash: スキップ (hardware not enabled. Set PICO_HARDWARE=1 to enable hardware operations.)。前提条件がないため未実行です。
- HIL: スキップ (hardware not configured)。実機未接続または未設定です。
- UART: スキップ (hardware not enabled. Set PICO_HARDWARE=1 to enable hardware operations.)。前提条件がないため未実行です。
- GDB: スキップ (hardware not enabled. Set PICO_HARDWARE=1 to enable hardware operations.)。前提条件がないため未実行です。
- Logic Analyzer UART: スタブ結果です。実測値ではありません (logic analyzer UART not enabled. Set PICO_LOGIC_UART=1 to enable UART capture, or PICO_LOGIC_ANALYZER=1 to enable all logic captures. Using sample decode output.)。
- Logic Analyzer I2C: スタブ結果です。実測値ではありません (logic analyzer I2C not enabled. Set PICO_LOGIC_I2C=1 to enable I2C capture, or PICO_LOGIC_ANALYZER=1 to enable all logic captures. Using sample decode output.)。

---
Generated by `scripts/summarize_evidence.py`. Judgments must be based on the evidence files above, not on this summary alone (see docs/operations/TEST_EVIDENCE_POLICY.md).
